kind: pipeline
type: docker
name: 'Build and test on ARM 64 bit'

platform:
  os: linux
  arch: arm64

steps:
- name: 'Build & Test  on ARM 64 bit'
  image: 'debian:stretch'
  commands:
    - bash -e build-and-test-in-docker.sh

---
kind: pipeline
type: docker
name: 'Build and test on ARM 32 bit'

platform:
  os: linux
  arch: arm

steps:
  - name: 'Build & Test on ARM 64 bit'
    image: 'debian:stretch'
    commands:
      - bash -e build-and-test-in-docker.sh

---
kind: pipeline
type: docker
name: 'Build and test on x64'

platform:
  os: linux
  arch: amd64

steps:
  - name: 'Build & Test on x64'
    image: 'debian:stretch'
    commands:
      - bash -e build-and-test-in-docker.sh

